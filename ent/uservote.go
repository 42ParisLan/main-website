// Code generated by ent, DO NOT EDIT.

package ent

import (
	"base-website/ent/component"
	"base-website/ent/user"
	"base-website/ent/uservote"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserVote is the model entity for the UserVote schema.
type UserVote struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserVoteQuery when eager-loading is set.
	Edges                UserVoteEdges `json:"edges"`
	component_user_votes *int
	user_user_votes      *int
	selectValues         sql.SelectValues
}

// UserVoteEdges holds the relations/edges for other nodes in the graph.
type UserVoteEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Component holds the value of the component edge.
	Component *Component `json:"component,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserVoteEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// ComponentOrErr returns the Component value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserVoteEdges) ComponentOrErr() (*Component, error) {
	if e.Component != nil {
		return e.Component, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: component.Label}
	}
	return nil, &NotLoadedError{edge: "component"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserVote) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case uservote.FieldID:
			values[i] = new(sql.NullInt64)
		case uservote.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case uservote.ForeignKeys[0]: // component_user_votes
			values[i] = new(sql.NullInt64)
		case uservote.ForeignKeys[1]: // user_user_votes
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserVote fields.
func (_m *UserVote) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case uservote.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case uservote.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case uservote.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field component_user_votes", value)
			} else if value.Valid {
				_m.component_user_votes = new(int)
				*_m.component_user_votes = int(value.Int64)
			}
		case uservote.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_user_votes", value)
			} else if value.Valid {
				_m.user_user_votes = new(int)
				*_m.user_user_votes = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserVote.
// This includes values selected through modifiers, order, etc.
func (_m *UserVote) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserVote entity.
func (_m *UserVote) QueryUser() *UserQuery {
	return NewUserVoteClient(_m.config).QueryUser(_m)
}

// QueryComponent queries the "component" edge of the UserVote entity.
func (_m *UserVote) QueryComponent() *ComponentQuery {
	return NewUserVoteClient(_m.config).QueryComponent(_m)
}

// Update returns a builder for updating this UserVote.
// Note that you need to call UserVote.Unwrap() before calling this method if this UserVote
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserVote) Update() *UserVoteUpdateOne {
	return NewUserVoteClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserVote entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserVote) Unwrap() *UserVote {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserVote is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserVote) String() string {
	var builder strings.Builder
	builder.WriteString("UserVote(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserVotes is a parsable slice of UserVote.
type UserVotes []*UserVote
